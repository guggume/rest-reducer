import { LIST, ITEMS, DATA } from './constants';

export const defaultState = {
  [LIST]: {},
  [ITEMS]: {},
};

export const defaultList = {
  [DATA]: [],
};

export const defaultItem = {
  [DATA]: {},
};
